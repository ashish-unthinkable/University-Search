if(data?.profile) {
    const name = data?.profile?.name ?? "";
    console.log(name)
    return true
}else{
    return false
}
 const data=false;
 data && 'if'

 return <div>{ramesh()}</div>

 const handle = () => {

    if(data.profile) {
        const name = data.profile.name ?? "";
        console.log(name)
        return true
    }

 }

 data?.[0];



 const obj={
    a:10
 }
 obj.a?.();